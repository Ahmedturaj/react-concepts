import React from 'react';

const Device = (props) => {
    return (
        <div>
            <h1>This is a Vite-React Project</h1>
            <p>I have a phone , name:{props.name}</p>
        </div>
    );
};

export default Device;